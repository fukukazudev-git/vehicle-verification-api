# docker compose がこのディレクトリの .env を自動読み込みします。
# このファイルを .env にコピーして値を設定してください（.env はコミットしない）。
# 本番環境では必ず開発用と異なる強力なパスワードを設定すること。

MYSQL_ROOT_PASSWORD=rootpassword
MYSQL_DATABASE=verification_db
MYSQL_USER=appuser
MYSQL_PASSWORD=apppassword
